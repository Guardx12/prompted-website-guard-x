import type { Metadata } from "next"

export const metadata: Metadata = {
  title: "Contact GuardX | Website Design Services",
  description:
    "Contact GuardX to get your professional website built and improve your online presence.",
}

export default function ContactLayout({ children }: { children: React.ReactNode }) {
  return children
}
